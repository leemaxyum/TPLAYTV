export type HubTopic = { id: string; title: string; summary: string; realWorld: string; learn: string; practice: string; solution: string; flashcard: { question: string; answer: string }; quiz: { question: string; options: string[]; correct: number; explanation: string } };
export type HubTrack = { title: string; description: string; topicCount: number; topics: HubTopic[] };

const make = (id: string, title: string, summary: string, realWorld: string, learn: string, practice: string, solution: string, question: string, answer: string, options: string[], correct: number, explanation: string): HubTopic => ({ id, title, summary, realWorld, learn, practice, solution, flashcard: { question, answer }, quiz: { question, options, correct, explanation } });

export const hubTracks: HubTrack[] = [
  { title: "Programming Languages Library", description: "Understand the ideas beneath Python, C++, and every program you will write.", topicCount: 25, topics: [
    make("pl-why", "Why programming languages exist", "Programming languages let humans express precise intentions while hiding irrelevant machine detail.", "A data team can prototype an analysis in Python; a game engine may choose C++ for tight control over performance. Both begin with the same problem: turn intent into repeatable behaviour.", "A CPU executes a tiny instruction set, not English ideas such as 'count each word'. Languages provide names, functions, data structures, errors, and libraries. They are trade-offs: convenience, speed, safety, control, ecosystem, and team familiarity all matter. There is no universally best language; there is a best fit for a constraint.", "A phone app needs responsiveness, a scientist needs a prototype tomorrow, and a safety-critical controller needs predictable behaviour. Name the quality that should dominate each choice.", "Responsiveness can prioritise performance; rapid research prioritises iteration speed and libraries; safety-critical work prioritises correctness checks and predictable runtime behaviour. The requirement comes before the language label.", "What is a programming language fundamentally for?", "It is a precise medium for expressing human intent as executable behaviour.", ["Decorating code", "Expressing executable intent", "Replacing hardware", "Making all programs fast"], 1, "Languages bridge human problem-solving and machine execution, each with different trade-offs."),
    make("pl-runtime", "Source code, compilers, and runtimes", "Trace the path from a text file to a running program without relying on the oversimplified compiled-versus-interpreted myth.", "A C++ build typically finds many mistakes before an executable exists. Python commonly discovers some mistakes only when a program reaches the relevant line. Modern tools can use bytecode, native code, and just-in-time compilation together.", "Source code is text. A parser checks its structure; a compiler may transform it ahead of time into machine code or intermediate code; a linker combines pieces and libraries. A runtime is machinery active while code runs, such as exception handling, memory management, library support, and scheduling. 'Interpreted' and 'compiled' describe workflows, not permanent personality traits.", "Why might a Python program reach a line and then fail while a C++ program fails before it starts?", "Python commonly performs more checks while executing paths. C++ compilation checks syntax and many type/interface constraints before producing an executable. Logic errors can survive both workflows.", "What does linking do?", "It combines compiled pieces and referenced libraries into a runnable program.", ["Formats source text", "Combines program pieces and libraries", "Deletes memory", "Runs only Python"], 1, "Linking resolves separately compiled code and libraries into a program."),
    make("pl-syntax-semantics", "Syntax, semantics, and bugs", "Separate code that is grammatically valid from code that actually means the intended thing.", "A payment condition can compile perfectly while charging the wrong customer; a loop can run without crashing while missing the last record. Those are semantic failures, not syntax failures.", "Syntax is grammar: are tokens arranged in a form the language accepts? Semantics is meaning: what behaviour does accepted code produce? Tests target behaviour, not only compilation. When debugging, state the expected outcome, make the smallest failing example, inspect the actual state, then repair the broken assumption.", "A loop uses `range(len(items) - 1)` but should inspect every item. Is that syntax or semantics, and what is the smallest test?", "It is semantic: the code is valid but omits the final item. Test a one-item list; the loop runs zero times, exposing the off-by-one error immediately.", "Can syntactically valid code still be wrong?", "Yes. Syntax checks grammar, while semantics concerns behaviour and meaning.", ["No, compilers prevent every bug", "Yes, valid grammar can express wrong behaviour", "Only in Python", "Only after deployment"], 1, "A program may be accepted by the language but violate the problem's requirements."),
    make("pl-types", "Types and data models", "Understand types as promises about values and operations, not as annoying declarations.", "A temperature, account balance, user ID, and date may all look like numbers or text, but treating them interchangeably creates bugs. Types and data models communicate what values represent and what operations are valid.", "A type describes a set of values and permitted operations. Static checks catch many mismatches before execution; dynamic checks occur while a program runs. Neither replaces reasoning. A data model is the shape of the information: a list preserves order, a map associates keys with values, and a record groups related fields.", "You need each student's name, email, and completed-topic count. Why is a named record/map safer than three parallel lists?", "Related information stays together and labels make intent explicit. Parallel lists can fall out of alignment, silently pairing the wrong email with a name.", "What does a type communicate?", "Which values a thing may hold and which operations make sense.", ["Only memory size", "Valid values and operations", "A colour theme", "A compiler version"], 1, "Types are constraints and documentation for meaningful program operations."),
    make("pl-memory", "Memory, values, references, and ownership", "Predict when changing one variable changes another and why lifetime matters.", "A shared list can accidentally change a UI state in two places. In C++, lifetime mistakes can become crashes or corrupted data. Understanding aliasing is a practical debugging skill.", "A value is data; a variable is a name bound to data or a reference to an object. When two names refer to one mutable object, a change through either is visible through both. Ownership answers who is responsible for a resource's lifetime. Python automates much memory management; C++ makes lifetime more explicit, with RAII and smart pointers helping express safe ownership.", "In Python, `a = [1, 2]`; `b = a`; `b.append(3)`. What is `a`, and why?", "`a` is `[1, 2, 3]`. Assignment copied the reference, not the list, so both names reach the same mutable object. Use a deliberate copy when independent state is required.", "What is aliasing?", "Multiple names or references accessing the same object.", ["Deleting a variable", "Multiple paths to one object", "A sorting method", "A type error"], 1, "Aliasing explains why a mutation through one name can be observed through another."),
    make("pl-paradigms", "Programming paradigms", "Recognise imperative, object-oriented, functional, and declarative styles as tools for organising thought.", "A UI updates state imperatively, a domain model may use objects, a data pipeline can transform collections functionally, and SQL declares the data wanted rather than each retrieval step.", "A paradigm is a recurring way to structure programs. Imperative code states steps and changing state. Object-oriented design groups state with behaviour. Functional style favours transformations and explicit inputs/outputs. Declarative style specifies a result while a system chooses many implementation details. Real programs often combine them.", "Match a report query, a game loop, and a collection transformation to the style most naturally useful for each.", "A report query is often declarative (SQL); a game loop is mostly imperative because time and state change; transforming a collection suits functional ideas such as map, filter, and reduce. The best style is dictated by clarity and constraints, not ideology.", "What makes declarative programming different?", "It specifies the desired result more than the exact sequence of steps.", ["It uses no variables", "It describes desired results", "It only works in C++", "It cannot access data"], 1, "Declarative systems such as SQL let the implementation choose many execution details."),
    make("pl-interfaces", "APIs, contracts, and compatibility", "Treat a function or module boundary as a promise that other code relies on.", "Changing an API response from a number to a string can break a mobile app even though the server still runs. Compatibility is a product concern as much as a coding concern.", "An interface includes names, accepted inputs, return shapes, errors, and observable behaviour. A contract states what a caller may assume and what the implementation requires. Prefer additive changes, version deliberate breaking changes, and keep error behaviour documented. Tests at a boundary protect users of your code.", "A function `convert(amount, currency)` previously returns a number. What should you consider before returning `{ value, currency }` instead?", "Existing callers may add the result to another number and fail. Add a new function or versioned endpoint, update consumers deliberately, and document the migration. A more expressive result is not automatically backward compatible.", "Why can a small API change be risky?", "Other code may rely on its current contract.", ["It changes file size", "Callers may rely on existing behaviour", "APIs never have tests", "Only browsers use APIs"], 1, "A boundary's contract is part of the program even when it is not written in a type declaration."),
    make("cpp-toolchain", "C++ toolchain and project anatomy", "Build a mental model of headers, source files, compilation, linking, and the executable.", "Large C++ applications split code across many source files and link third-party libraries. Build errors often tell you which stage misunderstood the program.", "A `.cpp` file contains implementation. A header commonly declares types and functions for other translation units. Each source file is compiled separately; the linker then resolves declared symbols to definitions. Include guards or `#pragma once` prevent repeated header inclusion. Build tools coordinate compiler flags and dependencies.", "A function is declared in a header but has no matching definition in any compiled source file. Which stage usually reports the problem?", "The linker reports an unresolved symbol because compilation can accept the declaration, but linking cannot find the implementation promised by it.", "What does the C++ linker primarily resolve?", "References from declarations to their definitions across compiled files and libraries.", ["Keyboard input", "Symbols across compiled units", "Only syntax", "JSON files"], 1, "The compiler checks each translation unit; the linker joins them into a complete program."),
    make("cpp-values", "C++ values, references, and const", "Use C++ bindings intentionally so mutation and copying are visible in the code.", "Passing a large image buffer by value can copy megabytes; passing a mutable reference can let a helper unexpectedly alter the caller's data. `const` documents and enforces intent.", "A value owns its own object. A reference is another name for an existing object. `const T&` commonly passes a non-mutating view without a copy. `T&` permits mutation. A pointer can be null and can be reseated; a reference normally must bind to a valid object when created. Prefer the narrowest capability a function needs.", "Choose parameter styles for: printing a large string, adding an item to a vector, and optionally receiving a configuration object.", "Use `const std::string&` for printing without copying or mutating; `std::vector<Item>&` to add an item; a pointer or `std::optional`-style representation when absence is meaningful. The parameter type communicates allowed effects.", "Why use `const T&` for a read-only large input?", "It avoids a copy while preventing mutation through that parameter.", ["It forces a copy", "It avoids copying and mutation", "It creates a null pointer", "It deletes the object"], 1, "Const references express borrowed, read-only access."),
    make("cpp-raii", "C++ RAII and resource safety", "Use object lifetime to make cleanup automatic and reliable.", "A file handle, mutex lock, socket, or heap allocation must be released even when a function returns early or throws. RAII makes that cleanup structural rather than a remembered chore.", "RAII means Resource Acquisition Is Initialization: acquire a resource in an object's constructor and release it in its destructor. Local objects are destroyed automatically when their scope ends. Use `std::vector`, `std::string`, `std::unique_ptr`, and lock guards instead of raw `new`/`delete` and manual unlock paths.", "Why is a lock guard safer than calling `lock()` followed later by `unlock()`?", "The guard unlocks when the scope exits even if an exception or early return happens. Manual cleanup has paths that humans forget to write.", "What guarantees RAII cleanup?", "Object destruction at the end of scope.", ["A garbage collector", "Object destruction at scope exit", "A global variable", "A network request"], 1, "RAII turns cleanup into a property of scope and object lifetime."),
    make("cpp-smart-pointers", "C++ ownership and smart pointers", "Choose ownership explicitly instead of hiding it behind raw pointers.", "A game entity can have one owner while UI observers merely refer to it. Confusing those relationships causes leaks, double frees, or use-after-free bugs.", "`std::unique_ptr<T>` expresses one owner and moves rather than copies. `std::shared_ptr<T>` expresses shared ownership but adds reference counting and can form cycles. `std::weak_ptr<T>` observes shared state without extending its lifetime. A raw pointer or reference often represents a non-owning borrowed view, not an ownership claim.", "A factory creates an object with one clear owner and returns it to a caller. Which smart pointer is the default, and why?", "Use `std::unique_ptr` because it makes sole responsibility clear and has minimal overhead. Reach for shared ownership only when the domain genuinely has multiple co-owners.", "What does `unique_ptr` communicate?", "Exactly one owner is responsible for the object's lifetime.", ["Everyone owns the object", "One owner controls lifetime", "The object cannot be destroyed", "The object is stored on disk"], 1, "Unique ownership is the simplest and safest ownership model when it matches reality."),
    make("cpp-containers", "C++ standard containers and algorithms", "Use the STL to express intent instead of rebuilding basic data structures by hand.", "A service can sort records, find a matching user, and transform measurements using well-tested library algorithms. Clear standard tools reduce bugs and make reviews faster.", "`std::vector` is the default growable sequence; `std::array` has fixed size; `std::unordered_map` provides average fast key lookup; `std::map` keeps ordered keys; `std::set` stores unique ordered values. Prefer algorithms such as `std::find`, `std::sort`, and `std::transform` when they state the operation directly. Complexity and invalidation rules still matter.", "You need ordered scores, then frequent access by index. Which container is a good default and why?", "A `std::vector` is a strong default: it preserves order, supports direct indexing, works with `std::sort`, and has contiguous storage. Insertions in the middle may be costly, which is a trade-off to revisit only if measurements require it.", "What is the usual default sequence container in modern C++?", "`std::vector`.", ["A raw array always", "std::vector", "std::shared_ptr", "std::mutex"], 1, "Vectors provide a flexible contiguous sequence and integrate with the standard algorithms."),
    make("cpp-errors", "C++ errors, exceptions, and results", "Model expected failure separately from programmer mistakes and resource cleanup.", "Reading a missing settings file is an expected operational failure; violating an internal invariant is a defect that should be investigated. Different failures need different handling.", "Exceptions can separate error paths from normal work and cooperate with RAII cleanup. For expected outcomes, an explicit result such as `std::optional<T>` or `std::expected<T, E>` can make absence or failure visible in the type. Never ignore an error merely to keep a program moving; decide what recovery is safe.", "A lookup may not find a user, and that is normal. Why might `optional<User>` be clearer than throwing?", "Absence is part of the expected result, so the return type forces callers to handle present versus absent values without treating routine control flow as an exceptional event.", "When is an explicit result type useful?", "When failure or absence is an expected part of normal control flow.", ["Only for syntax errors", "For expected failure or absence", "Never in C++", "Only in databases"], 1, "Types can make a caller confront expected outcomes rather than forget them."),
    make("python-cpp", "Python and C++: choosing the trade-off", "Compare Python and C++ through requirements instead of stereotypes.", "Python can accelerate a data experiment; C++ can give a latency-sensitive component fine-grained control. Many real products use both through service boundaries or extension modules.", "Python generally favours concise expression, dynamic inspection, and a rich ecosystem. C++ favours predictable low-level control, ahead-of-time optimisation, and explicit lifetime models. Python is not 'slow' in every system and C++ is not automatically fast; profiling, algorithms, I/O, data layout, and deployment constraints decide the result.", "A team needs to validate a research idea in two days, then later process millions of events under tight latency. Describe a sensible language strategy.", "Prototype the assumptions and data flow quickly in Python, measure the actual bottleneck, then move only the constrained component to C++ if profiling justifies the complexity. Keep the boundary small and test it from both sides.", "What should decide a language choice?", "The system's constraints and the team's ability to deliver safely.", ["A popularity contest", "Requirements and constraints", "File extension length", "Only raw benchmark speed"], 1, "Languages are engineering trade-offs, not permanent identities."),
    make("pl-testing", "Testing across languages", "Build confidence with small, repeatable checks that express behaviour.", "A currency conversion function, parser, and ranking algorithm can all look correct on one hand-picked input yet fail at boundaries. Tests preserve knowledge when code changes later.", "Arrange inputs, act by calling the unit, then assert an observable result. Include normal, boundary, and invalid cases. Unit tests isolate a small piece; integration tests check boundaries; property tests generate many cases from an invariant. A failing test is evidence about behaviour, not a personal verdict.", "For a function that returns the maximum item of a non-empty list, name three tests.", "Test a typical unsorted list, one item, and values including negatives or duplicates. State whether empty input is rejected or returns an optional result, then test that contract too.", "What should a good test assert?", "An observable behaviour promised by the contract.", ["How the code is indented", "Observable promised behaviour", "Only that it compiles", "The programmer's intention without running"], 1, "Tests give executable examples of a program's contract."),
    make("pl-practice-plan", "Practice loops for interview problems", "Turn problem practice into deliberate skill-building instead of random question collecting.", "Interview platforms reward pattern recognition, but the transferable skill is explaining constraints, choosing a representation, and checking correctness under pressure.", "Use a loop: restate the problem, list examples and constraints, propose a brute-force baseline, identify the bottleneck, choose a data structure or invariant, implement in small steps, test edge cases, then write a short postmortem. Re-solve missed problems after a delay without reading the answer. Track patterns such as two pointers, hashing, stacks, binary search, trees, graphs, and dynamic programming.", "You solve a problem only after seeing the answer. What should tomorrow's review contain?", "Close the solution, reproduce the key invariant or state transition from memory, solve a nearby variation, and write one sentence about the clue that should trigger the pattern next time. Memorising code without the trigger does not transfer.", "What is the point of a brute-force solution?", "It establishes correctness and exposes the bottleneck an improvement must address.", ["It is always the final answer", "It reveals the baseline and bottleneck", "It avoids understanding", "It replaces tests"], 1, "A baseline turns optimisation into a reasoned comparison rather than a guess."),
    make("js-runtime", "JavaScript runtime and the browser", "Understand JavaScript as a language that runs inside a host such as a browser or Node.js.", "The same JavaScript can validate a form in a browser or serve a small API in Node.js, but the surrounding host provides different capabilities such as the DOM, file system, timers, and network APIs.", "JavaScript defines language features such as values, functions, objects, and promises. A host provides the environment. Browsers expose `document`, events, and rendering; Node.js exposes process and file APIs. Code that depends on a host capability is not automatically portable to another host.", "Why does `document.querySelector` work in a browser page but not automatically in a Node.js script?", "`document` belongs to the browser's DOM environment, not to JavaScript itself. Node.js provides different host APIs. Separate language features from host features when debugging portability.", "What provides the DOM API?", "The browser host environment, not the JavaScript language specification alone.", ["Any JavaScript value", "The browser host", "The C++ compiler", "A CSS file"], 1, "A language runs inside an environment that supplies additional APIs."),
    make("js-values", "JavaScript values, equality, and coercion", "Avoid surprising comparisons by understanding JavaScript's value types and conversions.", "A form field supplies text even when it looks like a number. If a checkout total accidentally joins strings instead of adding numbers, users see a product bug rather than a language lesson.", "JavaScript has primitives such as string, number, boolean, null, undefined, bigint, and symbol, plus objects. Prefer strict equality `===`, which does not silently coerce values. Convert external input deliberately with tools such as `Number()` and check whether a result is valid. Avoid relying on clever implicit conversion rules.", "A form gives `quantity` as the string `'2'`. Why can `quantity + 1` surprise you, and what is a safer approach?", "It can produce `'21'` because string concatenation is possible. Convert intentionally with `const count = Number(quantity)` and reject invalid input before calculating. Data from the outside needs a deliberate boundary.", "Why prefer `===` to `==` in ordinary application code?", "It avoids implicit type coercion during comparison.", ["It is shorter", "It avoids coercive comparisons", "It compares only strings", "It makes code asynchronous"], 1, "Strict equality makes the types involved in a comparison explicit."),
    make("js-objects", "JavaScript objects, arrays, and immutability", "Model records and sequences without accidental shared mutation.", "A UI state update that mutates a shared object can make a screen fail to re-render or silently change another component's data. Copying only the layer you change makes updates easier to reason about.", "Objects map property names to values; arrays are ordered objects designed for sequences. Assignment shares an object reference. Use spread syntax such as `{ ...profile, name: nextName }` or `[...items, newItem]` to create a shallow copy when a new state value is required. Deeply nested state needs careful modelling because a shallow copy does not copy nested objects.", "You have `const profile = { settings: { theme: 'light' } }`. Does `{ ...profile }` make `settings` independent?", "No. The outer object is copied but `settings` remains the same nested object. Copy the layer being changed as well: `{ ...profile, settings: { ...profile.settings, theme: 'dark' } }`.", "What kind of copy does object spread normally make?", "A shallow copy of the object's own properties.", ["A guaranteed deep copy", "A shallow copy", "No copy", "A disk backup"], 1, "Spread is useful for a new outer object, but nested references still need deliberate handling."),
    make("js-functions", "JavaScript functions, closures, and callbacks", "Use functions as values while keeping captured state understandable.", "Event handlers, timers, and collection transforms all pass functions to other functions. A closure can preserve private state for a counter or accidentally keep stale state if you do not understand what was captured.", "Functions are first-class values: they can be stored, passed, and returned. A closure is a function plus access to lexical variables from where it was created. Prefer small named functions for important behaviour, and be explicit about inputs rather than relying on a large hidden captured context.", "Why can a callback still access `topicId` after the outer function returned?", "The callback closes over its lexical environment, so the runtime preserves the binding as long as the callback can be used. This is powerful for encapsulation but demands care with changing state.", "What is a closure?", "A function that retains access to its lexical environment.", ["A closed browser tab", "A function plus lexical access", "A compiler warning", "A class-only feature"], 1, "Closures let functions carry context with them."),
    make("js-async", "Promises, async/await, and failure", "Write asynchronous JavaScript that handles both completion and failure paths.", "Loading a course catalog, saving progress, and calling an API take time. Good UI code communicates loading, success, empty, and failure states rather than assuming every request succeeds instantly.", "A promise represents a future result or failure. `await` pauses an async function's continuation until the promise settles; it does not freeze the entire browser. Use `try`/`catch` around awaited operations you can recover from, and consider cancellation or stale responses when users navigate quickly.", "A learner opens topic A, then immediately topic B; topic A's slower request finishes last. What bug can occur, and how can you defend against it?", "A stale response can overwrite topic B on screen. Track the current request/token, cancel when supported, or check that the response still matches the active topic before updating state.", "What does `await` wait for?", "A promise to settle with a result or an error.", ["The whole computer", "A promise's settlement", "Only CSS", "A C++ header"], 1, "Async/await gives promise-based work a readable sequential style while preserving asynchronous execution."),
    make("typescript-types", "TypeScript types and narrowing", "Use TypeScript to make program assumptions explicit before the browser runs the code.", "A lesson record with a missing quiz field can otherwise fail only when a learner opens it. A type makes the expected shape visible to the editor and compiler.", "TypeScript checks types while building but emits JavaScript for runtime. Describe shapes with interfaces or type aliases, then narrow unions with checks such as `typeof`, `in`, or discriminant fields. Do not trust a type annotation for unvalidated network data: runtime validation still protects the boundary.", "Why is `unknown` safer than `any` for JSON received from a server?", "`unknown` requires you to check the value before using it. `any` disables useful checks and lets an incorrect assumption travel deeper into the application.", "What does TypeScript primarily add to JavaScript development?", "Static checking of declared assumptions and program shapes.", ["A new browser", "Static type checking", "Automatic database storage", "Guaranteed runtime validation"], 1, "TypeScript catches many mismatches early but cannot validate untrusted runtime data by itself."),
    make("typescript-generics", "TypeScript generics and reusable shapes", "Preserve the relationship between inputs and outputs in reusable functions.", "A generic `first<T>(items: T[]): T | undefined` tells a caller that an array of lessons yields a lesson, while an array of scores yields a score. Losing that relationship makes helpers less safe.", "A generic parameter such as `T` is a placeholder chosen by each caller. Constraints such as `T extends { id: string }` require a minimum capability without discarding the rest of the type. Use generics when the same algorithm applies to many data types and the types should remain related.", "Why is `function first<T>(items: T[]): T | undefined` better typed than returning `unknown`?", "It retains the caller's specific element type. A lesson array returns a lesson-or-undefined, so autocomplete and later checks remain useful.", "What does a generic type parameter represent?", "A type chosen by the caller while preserving relationships in an API.", ["A fixed string", "A caller-specific type placeholder", "Only a number", "A runtime JSON validator"], 1, "Generics let one implementation safely work across related types."),
    make("web-components", "Events, state, and UI rendering", "Connect user actions to state changes and a deliberate screen update.", "A quiz button should update its answer state once, disable conflicting inputs, and render feedback. Treating UI elements as the only source of truth makes complex screens inconsistent.", "An event reports an interaction; state is the data the interface represents; rendering maps state to visible elements. Keep a single clear owner for each piece of state. On an event, validate intent, update state, then render the affected view. This pattern works with plain DOM code and with frameworks.", "A learner selects a quiz option. Name the three stages of a reliable update.", "Handle the click event, update quiz state with the answer/attempt status, then render the selected option and feedback from that state. Do not depend solely on a CSS class as the record of whether a question was answered.", "What should drive a UI render?", "The application's state.", ["Random DOM mutations", "Application state", "Only CSS animations", "The file name"], 1, "State-driven rendering keeps the visible interface and program data consistent."),
    make("pl-project", "Build: a local study tracker", "Combine language fundamentals in a small project with a concrete definition of done.", "A personal study tracker uses values, functions, file storage, error handling, tests, and an interface boundary—exactly the skills that make individual concepts useful together.", "Build a command-line or browser-local tracker that can add a topic, mark it complete, record focused minutes, and list the next item. Start with a tiny data model, write a function for each operation, save progress locally, and test invalid input. Keep scope narrow: a dependable small project teaches more than an unfinished giant one.", "What is the minimum viable definition of done for this project?", "A learner can create at least one task, complete it, close and reopen the app without losing saved data, and see a clear error for invalid input. Add automated tests for the core add/complete logic before decorative features.", "What makes a first project finished?", "A small, stated set of reliable user outcomes, not an endless feature list.", ["The most animations", "Reliable stated user outcomes", "Thousands of lines", "No error messages"], 1, "A definition of done converts an idea into a testable, shippable learning project."),
  ] },
  { title: "Programming Foundations", description: "Turn ideas into dependable programs.", topicCount: 20, topics: [
    make("py-errors", "Python errors, exceptions, and debugging", "Turn a failure into a small, explainable repair instead of guessing at code.", "A bad date, missing file, and failed network request are normal conditions in software. A program should distinguish expected bad input from a bug in its own logic.", "Read the traceback from bottom to top: exception type, message, then the relevant line. Reproduce the smallest failure before changing anything. Use `try` around an operation you genuinely expect can fail and catch the most specific error you can handle. Never use a bare catch-all to hide mistakes.", "A user enters `ten` where an integer is needed. What error should you handle, and what should the program do?", "`int('ten')` raises `ValueError`. Catch that exact error, explain what valid input looks like, and ask again or return a clear failure result. Do not catch every exception because a programming bug should remain visible.", "Why should exception handling be specific?", "Specific handling recovers expected failures without hiding unrelated bugs.", ["It makes code longer", "It avoids hiding real bugs", "Python requires it", "It prevents all errors"], 1, "A precise exception branch documents the failure your program can recover from."),
    make("py-files-json", "Python files and JSON", "Persist structured data safely and understand the boundary between a program and a file.", "A study tracker must survive closing the app. JSON is a common portable format for settings and simple records, while file errors and malformed data are real product cases.", "Use a context manager so a file closes even if an error occurs. Read external data as untrusted: validate its type and required fields before using it. JSON maps naturally to Python dictionaries/lists, but it cannot represent every Python object and should not be used to execute data.", "What steps should a study tracker take when loading its JSON progress file?", "Open safely with `with`, parse JSON, validate that the top-level value has the expected shape, provide an empty/default state if the file is absent, and show a recovery message for malformed data.", "Why validate JSON after parsing?", "Valid JSON can still have the wrong shape for your program.", ["JSON is always correct", "It may not match the expected data model", "Files cannot fail", "Validation is only for databases"], 1, "Syntax validity and application validity are different checks."),
    make("py-modules", "Python modules, packages, and environments", "Organise a project into reusable pieces without breaking other projects on your computer.", "A CLI tool can keep input parsing, business logic, and file storage in separate modules, letting each be tested independently. Virtual environments isolate exactly which packages a project needs.", "A module is a Python file. A package groups modules. Import the narrowest public interface you need and avoid circular imports. A virtual environment is a project-specific Python installation context; it prevents one project’s dependency upgrade from breaking another.", "Why is `from helpers import normalise_name` clearer than putting every function in one script?", "It communicates ownership and lets the application, tests, and helpers evolve independently. The import also makes the dependency explicit.", "What problem does a virtual environment solve?", "It isolates a project's dependencies from other projects.", ["It makes Python faster", "It isolates dependencies", "It compiles C++", "It stores passwords"], 1, "Environments reduce dependency conflicts and make a project reproducible."),
    make("py-objects", "Python objects and class design", "Know when a class clarifies an entity and when a simple function or dictionary is better.", "A study session has state such as a topic, start time, and completed minutes plus actions such as start, pause, and complete. Grouping stable state with its valid actions can protect invariants.", "Classes are templates for objects. Use them when data and behaviour belong together over time. Keep constructors simple, name methods by action, and enforce invariants near the data they protect. Do not make a class for every concept; plain functions and dictionaries are often clearer for simple transformations.", "Design the invariant for a `StudySession` that tracks minutes completed.", "Minutes should never be negative, and a completed session should not keep accumulating time without starting a new session. Methods such as `add_minutes` should reject invalid values rather than letting impossible state spread.", "When is a class useful?", "When an entity has stable state and related behaviour with invariants.", ["For every variable", "For state plus related behaviour", "Only for games", "Never in Python"], 1, "Classes help when they make a model and its valid operations easier to understand."),
    make("py-values", "Python values, names, and expressions", "Predict how Python evaluates an expression and binds a name before writing code.", "A checkout total, experiment setting, and game score are all values transformed by expressions and remembered through names.", "Python evaluates the right-hand expression first, then binds the result to a name. `total = price * quantity` is a statement about program state, not algebra. Use `type()` and small print statements while learning, but explain the value flow rather than trusting output blindly.", "Trace `price = 12; price = price + 3; discount = price * 0.1`. What are price and discount?", "price becomes 15, then discount becomes 1.5. The second assignment reads the old binding before rebinding `price`.", "What happens first in `x = y + 1`?", "Python evaluates `y + 1`, then binds x to that result.", ["x is created first", "The right expression is evaluated", "y is deleted", "A function is called automatically"], 1, "Assignment follows evaluate-then-bind order."),
    make("py-decisions", "Python decisions and Boolean logic", "Write conditions that explain a rule clearly and handle invalid cases without nesting confusion.", "A signup flow checks age, email verification, and account state before allowing an action. Missing one condition creates a real product bug.", "Comparisons produce Boolean values. Combine them with `and`, `or`, and `not`, then handle invalid or exceptional cases early. Read a condition aloud; if you cannot state its rule in English, split it into named Boolean variables.", "A learner may submit when they are enrolled and their assignment is not already submitted. Write the condition and its rejection rule.", "Use `enrolled and not submitted`. The rejection is `not enrolled or submitted`; De Morgan's law explains why the conjunction flips to an `or` when negated.", "When is `A and B` true?", "Only when both A and B are true.", ["When either is true", "Only when both are true", "Never", "Only for numbers"], 1, "An AND condition requires every required condition."),
    make("py-collections", "Python collections: lists, strings, maps, and sets", "Choose a collection by the question you need to answer, not by habit.", "A playlist needs order, a profile needs labelled fields, and a duplicate detector needs fast membership checks. One list cannot model all three cleanly.", "Lists preserve order and allow repeated values. Strings are immutable ordered characters. Dictionaries map unique keys to values. Sets hold unique values and make membership checks efficient on average. The collection choice is an engineering decision: what must be preserved, retrieved, or prevented?", "You must store a user's name, email, and completed lesson IDs, while checking whether a lesson was already completed. Propose a structure.", "Use a dictionary for named profile fields and a set for completed IDs: `{'name': ..., 'email': ..., 'completed': set()}`. Names are labelled, and repeated completion is naturally prevented.", "Which collection best models unique completed lesson IDs?", "A set.", ["A string", "A set", "A tuple of duplicates", "A float"], 1, "Sets encode uniqueness and efficient membership."),
    make("py-loops", "Python loops, invariants, and input safety", "Repeat work without losing track of what is already correct.", "Reporting totals, parsing rows, and checking every answer are loop problems. Most loop bugs come from an unstated assumption about progress or stopping.", "A loop invariant is a fact that stays true each iteration. For a sum, after processing k values, `total` equals the sum of those k values. Prefer `for` when iterating a known collection; use `while` when the stopping condition itself controls repetition. Validate input at the boundary before using it.", "Write the invariant for finding the largest value in a non-empty list.", "After processing k items, `largest` equals the greatest of those k items. Initialise it from the first item, then replace it only when a later item is larger.", "Why is a loop invariant useful?", "It states the fact that lets you reason about correctness at every step.", ["It makes loops faster", "It explains correctness", "It removes tests", "It avoids all variables"], 1, "Invariants turn a vague loop into a checkable process."),
    make("py-functions", "Python functions, contracts, and tests", "Design a small function whose behaviour is easy to call, test, and reuse.", "A grade calculator, parser, and API validator are safer when each task has a narrow, testable function instead of one giant script.", "A function contract names inputs, output, assumptions, and edge cases. Parameters bring values in; `return` sends a result out. Test normal, boundary, and invalid cases. A function that prints instead of returning is often harder to compose and test.", "Design `normalise_name(text)` for user names with outer spaces and inconsistent case. State three tests.", "A clear implementation is `return text.strip().title()`. Test a normal name, extra spaces, and an empty string. Then decide product behaviour for empty input instead of silently guessing.", "Why is a return value usually better than printing inside a utility function?", "The caller can test, combine, store, or display the result in different ways.", ["Printing is illegal", "Return values are composable", "It uses less memory always", "Functions cannot print"], 1, "Returning separates computation from presentation."),
    make("variables", "Variables and assignment", "Names refer to values; assignment evaluates the right side before rebinding the name.", "Counters power totals, inventory, and analytics.", "In Python, `score = score + 1` reads the current score, creates a new value, then binds score to it. This is different from algebraic equality.", "Trace `count = 4; count = count + 3`. What prints?", "It prints 7: the expression uses 4, produces 7, then count is rebound.", "What happens first in `x = x + 1`?", "The expression on the right is evaluated first.", ["x is erased", "The right side is evaluated", "x becomes a string", "The program always fails"], 1, "Assignment evaluates the expression on the right, then binds the name on the left."),
    make("control-flow", "Conditionals and Boolean logic", "A Boolean condition selects a path; guard clauses make invalid cases explicit.", "Validation and access control depend on complete conditions.", "Use `and`, `or`, and `not` to combine conditions. Place invalid cases first so the normal path remains easy to read.", "A user may submit only when account and payment are both valid. Write the condition.", "Use `has_account and fee_paid`. If either is false, submission should not proceed.", "What does `and` require?", "Both conditions must be true.", ["Either condition", "Both conditions", "Neither condition", "Only numbers"], 1, "A Boolean `and` is true only when each operand is true."),
    make("loops", "Loops and invariants", "Loops repeat a process; invariants are facts that remain true during repetition.", "Batch processing and file parsing rely on correct loop state.", "For a running total, the invariant is: after k items, total equals the sum of those k items. Start at zero and update once per item.", "What invariant describes a running total after processing k values?", "The total equals the sum of exactly the k processed values.", "Why is a loop invariant useful?", "It gives you a fact to check after every iteration.", ["It makes code longer", "It helps reason about correctness", "It removes all bugs", "It replaces tests"], 1, "Invariants make a loop's intended state explicit and testable."),
    make("functions", "Functions and decomposition", "A function is a small named transformation with inputs and an output.", "Services stay maintainable when validation, calculation, and formatting are separate.", "Parameters bring information in; `return` sends a result out. A well-named function should do one understandable job.", "Design `is_even(number)` and state its result for 14 and 9.", "Return `number % 2 == 0`; it gives True for 14 and False for 9.", "What does `return` do?", "It sends a result to the caller.", ["Prints only", "Stops all programs", "Sends a function result", "Creates a loop"], 2, "`return` finishes the function call and provides its result."),
    make("strings", "Strings and text", "A string is an ordered immutable sequence of characters.", "Text cleaning powers search, usernames, logs, CSV data, and every chat interface.", "Use slicing to select part of a string and methods such as `strip`, `lower`, `split`, and `join` to normalise text. Because strings are immutable, each transformation creates a new value.", "Normalise the input `  Ada Lovelace  ` into `ada lovelace`.", "Use `name.strip().lower()`. First remove surrounding whitespace, then convert case.", "Why does `text.lower()` not mutate text?", "Strings are immutable; it returns a new string.", ["It changes every file", "Strings are immutable", "lower only prints", "Python has no strings"], 1, "String methods return transformed values rather than changing the original string."),
    make("lists", "Lists and iteration", "A list is an ordered, mutable collection of values.", "Shopping carts, API results, batches of sensor readings, and students in a class are list-shaped data.", "Use `append` to grow a list and `for item in items` to process each value. Avoid changing a list while iterating over it unless you understand the consequences.", "Write the loop idea to compute the largest value in a non-empty list.", "Set `largest` to the first item. For each later item, replace it only when that item is larger. The invariant is that largest is the maximum seen so far.", "Which operation adds an item to the end of a Python list?", "append.", ["push", "append", "return", "split"], 1, "`append` adds one item to a list's end."),
    make("dictionaries", "Dictionaries and data modelling", "A dictionary gives meaningful names to related values or maps a key to an associated value.", "User profiles, configuration, counters, and JSON API payloads are usually dictionaries.", "Choose keys that answer a question: `profile['email']` communicates more than position 2 in a list. Use `get` when a key may be absent and decide explicitly what the fallback means.", "How would you safely read an optional `nickname` with `Anonymous` as fallback?", "Use `profile.get('nickname', 'Anonymous')`. It returns the stored value when present and the fallback when absent.", "What does a dictionary associate?", "Keys with values.", ["Only positions", "Keys with values", "Files with folders", "Loops with conditions"], 1, "A dictionary is a mapping from distinct keys to values."),
    make("comprehensions", "Comprehensions and transformations", "A comprehension expresses a new collection from an old one in one readable transformation.", "Data pipelines constantly filter, clean, and transform collections before analysis.", "Read a comprehension as: output expression, for each item, optionally if it passes a condition. Prefer a normal loop when the expression becomes hard to explain.", "Create squares of only even numbers from `numbers`.", "`[n * n for n in numbers if n % 2 == 0]`. The condition filters before the square is produced.", "What is the order in a list comprehension?", "Output expression, iteration, optional filter.", ["Filter then file IO", "Output, iteration, optional filter", "Class then object", "Only a loop"], 1, "Comprehensions make a collection transformation compact but should stay readable."),
    make("exceptions", "Errors and exceptions", "Exceptions signal that normal execution cannot continue with the current operation.", "A file may be missing, a network request may time out, or user input may not parse as a number.", "Catch only errors you expect and can handle. Keep the risky operation in `try`, recover in a precise `except`, and do not hide programming mistakes with a bare catch-all.", "A user enters `abc` where an integer is required. Which exception should your code anticipate?", "`ValueError`, because converting `abc` with `int()` cannot produce a valid numeric value.", "Why avoid `except Exception: pass`?", "It hides failures and makes debugging much harder.", ["It is too fast", "It hides useful failures", "Python forbids it", "It adds types"], 1, "Silent catch-alls turn visible bugs into mysterious wrong behaviour."),
    make("files", "Files and context managers", "Files persist bytes beyond one program run; context managers reliably release resources.", "Programs import datasets, save reports, write logs, and store small local configuration files.", "Use `with open(path, encoding='utf-8') as file:` so the file closes even if an error occurs. Treat external file contents as untrusted input that must be validated.", "Why is `with open(...) as file` safer than leaving a file open manually?", "The context manager closes the file reliably when the block exits, even if an exception happens.", "What does a context manager guarantee here?", "Resource cleanup.", ["Faster internet", "Resource cleanup", "Sorted output", "A database"], 1, "The `with` block owns the resource lifetime and closes it deterministically."),
    make("modules", "Modules and imports", "A module is a Python file that groups reusable names; imports make those names available elsewhere.", "Large programs separate domain logic, UI, data access, and tests into modules so changes stay local.", "Import the smallest public surface you need and avoid circular imports. A module should have one coherent responsibility, like `pricing` or `parsing`.", "Why split a 1,000-line script into modules?", "Each part gains a focused responsibility, which makes testing, reading, and changing it safer.", "What is a Python module?", "A file containing reusable Python definitions.", ["Only a package manager", "A reusable Python file", "A web browser", "A loop"], 1, "Modules are ordinary Python files used as organised units of code."),
    make("testing", "Testing and debugging", "A test states an expected behaviour and checks it automatically; debugging finds the cause when reality differs.", "Payment calculations, data transforms, and machine-learning preprocessing need repeatable checks against regressions.", "Test normal cases, edge cases, and invalid inputs. When debugging, reduce the failing example, inspect assumptions, and add a test before fixing the bug so it cannot quietly return.", "Name an edge case for `is_even(n)`.", "Zero and negative numbers are useful edges: `is_even(0)` should be True and `is_even(-3)` False. Both reveal whether the rule relies on an accidental positive-only assumption.", "What is a good first debugging move?", "Make the failing input as small as possible.", ["Rewrite everything", "Reduce the failing example", "Ignore the error", "Add random code"], 1, "A minimal reproducible example exposes the exact broken assumption."),
    make("objects", "Objects and classes", "An object groups state with behaviour; a class describes how related objects are created.", "A game player, bank account, course enrolment, or ML experiment has data plus operations that belong together.", "Start with plain functions and dictionaries when they are enough. Introduce a class when an entity has stable state and several operations that must preserve its invariants.", "What invariant might a BankAccount object preserve?", "Its balance should change only through validated deposits and withdrawals; invalid operations must not silently create an impossible state.", "When is a class helpful?", "When stable state and related behaviour belong together.", ["For every variable", "For related state and behaviour", "Only for web pages", "Never"], 1, "Classes model an entity whose data and operations must remain consistent."),
  ] },
  { title: "Discrete Mathematics", description: "Reason precisely about logic, sets, proof, and counting.", topicCount: 18, topics: [
    make("logic", "Propositional logic", "Logic combines statements whose truth can be evaluated.", "Digital circuits, tests, and rules engines are built from logical conditions.", "A proposition is a statement that is true or false. Use truth tables to inspect `and`, `or`, implication, and negation.", "When is `A and B` true?", "Only when both A and B are true.", "What is a proposition?", "A statement that is true or false.", ["A question", "A truth-evaluable statement", "A program", "A number"], 1, "A proposition has a definite truth value."),
    make("induction", "Mathematical induction", "Induction proves a claim for every natural number by linking each case to the next.", "Recursive algorithms and recurrence arguments use the same structure.", "Prove a base case, assume the claim for k, then show that assumption implies the claim for k+1.", "Name the two essential induction steps.", "Base case and inductive step.", "What does the inductive hypothesis assume?", "The statement holds for an arbitrary k.", ["The final answer", "The claim for k", "The claim is false", "A random example"], 1, "The hypothesis assumes the claim at k so you can establish k+1."),
    make("sets", "Sets and relations", "A set is an unordered collection of distinct elements; relations describe which elements are connected.", "Database filters, permissions, graph edges, and feature categories are set operations in practice.", "Use union for either set, intersection for both, and difference for items in one set but not another. Write a small example before manipulating symbols.", "If A={1,2,3} and B={3,4}, what is A intersect B?", "{3}. Intersection keeps only values present in both sets.", "What does intersection keep?", "Elements shared by both sets.", ["All elements", "Shared elements", "Only new elements", "No elements"], 1, "Intersection means logical AND for membership."),
    make("functions-math", "Functions and inverse thinking", "A mathematical function maps each permitted input to exactly one output.", "Data transforms, program interfaces, and ML models are all functions from inputs to outputs.", "Ask whether a rule is one-to-one and what its domain permits. An inverse undoes a function only when each output identifies one input.", "Can f(x)=x squared have a single inverse on all real numbers?", "No: both 2 and -2 map to 4. Restricting the domain to non-negative numbers makes an inverse possible.", "Why can a function lack an inverse?", "Different inputs can produce the same output.", ["It has no numbers", "Outputs may not identify one input", "It is always linear", "It uses sets"], 1, "An inverse needs a unique way to recover the original input."),
    make("counting", "Counting principles", "Counting turns a sequence of choices into multiplication and alternative cases into addition.", "Password spaces, test cases, probability, and algorithm analysis all start with careful counting.", "Multiply independent staged choices; add disjoint alternatives. State whether order matters before choosing permutations or combinations.", "A code has 3 digits, each 0–9, with repetition allowed. How many codes?", "10 × 10 × 10 = 1000. Each position independently has 10 choices.", "When do you multiply counts?", "For sequential independent choices.", ["For disjoint alternatives", "For sequential choices", "Never", "Only with primes"], 1, "The product rule combines choices made in sequence."),
    make("modular", "Modular arithmetic", "Modular arithmetic keeps remainders after division, like numbers on a clock.", "Checksums, calendars, cyclic buffers, cryptography, and array wraparound use modular reasoning.", "a mod n is the remainder after division by n. Values that differ by a multiple of n are equivalent modulo n.", "What is 23 mod 7?", "2, because 23 = 3×7 + 2.", "What does `a mod n` return?", "The remainder after division by n.", ["The quotient", "The remainder", "A negative only", "A random value"], 1, "Modulo measures the amount left after whole groups of n."),
    make("proofs", "Proof strategies", "A proof is a chain of justified claims, not a collection of examples.", "Correctness arguments for security rules and algorithms must work beyond the cases you happened to test.", "Use direct proof when definitions lead forward, contradiction when an assumption clashes with a fact, and contrapositive when reversing an implication clarifies it.", "To prove 'if n squared is even, n is even,' which related statement is often easier?", "Its contrapositive: if n is odd, then n squared is odd. Write n=2k+1 and expand.", "What is a counterexample used for?", "To disprove a universal claim.", ["To prove every claim", "To disprove a universal claim", "To sort data", "To define a loop"], 1, "One valid counterexample defeats an 'all' statement."),
  ] },
  { title: "Algorithms and Data Structures", description: "Choose the right representation and reason about scale.", topicCount: 22, topics: [
    make("complexity", "Complexity and scale", "Big-O describes the growth of dominant work as input grows.", "It decides whether a feature works for 100 records or 10 million.", "O(n) scans items once; O(n squared) often compares every pair; O(log n) repeatedly halves a valid search space.", "A nested loop compares every pair of n names. What is the common complexity?", "O(n squared). A set can often reduce duplicate detection to expected O(n).", "What does O(n) describe?", "Work that grows roughly linearly with input size.", ["Constant work", "Linear growth", "Always slow", "No memory use"], 1, "O(n) means the dominant operation is done once per input item."),
    make("binary-search", "Binary search", "Binary search discards half of a sorted search interval at a time.", "Databases and autocomplete use ordered/indexed data for fast lookup.", "Maintain the invariant that if the target exists, it lies in the current low-to-high interval. Compare the middle, then remove the impossible half.", "Why is binary search invalid on unsorted data?", "The middle comparison gives no reliable direction, so discarding half may discard the target.", "What prerequisite does binary search need?", "Sorted data.", ["A queue", "Sorted data", "A GPU", "Random input"], 1, "Order makes a middle comparison informative."),
    make("hash-maps", "Hash maps", "Hash maps associate keys with values for fast average lookup.", "Counting events, caching, and indexing use key-value lookup constantly.", "A hash function directs a key toward a location. Collisions need handling, but a dictionary still gives expected constant-time access in common cases.", "How would you count words in a document?", "Use a dictionary and update `counts[word] = counts.get(word, 0) + 1`.", "What does a hash map store?", "Key-value associations.", ["Only sorted numbers", "Key-value pairs", "A single stack", "Images only"], 1, "A hash map maps a distinct key to an associated value."),
    make("arrays", "Arrays and indexing", "An array keeps values in a defined order, so a position can be read directly.", "Timelines, image pixels, sensor samples, and game boards are naturally ordered collections.", "Indexes begin at zero in Python. Reading item i is constant time, but inserting at the front shifts later items, which costs linear time.", "For `scores = [7, 9, 4]`, what are `scores[0]` and `scores[-1]`?", "They are 7 and 4. Index zero is the first item; negative one is the final item in Python.", "What is the usual cost of reading an array item by index?", "Constant time.", ["O(1)", "O(n)", "O(n squared)", "Impossible"], 0, "An array can calculate the location of an indexed item directly."),
    make("sorting", "Sorting and comparison", "Sorting establishes an order so later operations can reason efficiently about data.", "Leaderboards, date feeds, ranking, and database query plans all depend on ordering.", "Comparison sorts repeatedly ask which of two items comes first. A practical built-in sort is usually preferable; learn elementary sorts to understand invariants, not to replace mature libraries.", "Why is it useful to sort records by date before searching a date range?", "Dates become contiguous, so binary search can find the boundaries instead of scanning every record.", "What does sorting create?", "A consistent order based on a comparison rule.", ["Randomness", "A comparison-based order", "A hash collision", "More memory only"], 1, "Sorting places values into an order that later algorithms can exploit."),
    make("recursion", "Recursion and base cases", "Recursion solves a problem by reducing it to smaller instances of the same problem.", "Folder traversal, parsers, tree search, and divide-and-conquer algorithms use recursive structure.", "Every recursive function needs a base case that stops and a reduction that moves measurably toward it. Trace one small input by hand before trusting it.", "Give a recursive definition of factorial for non-negative n.", "Base: factorial(0) = 1. Reduction: factorial(n) = n × factorial(n - 1) for n > 0.", "What prevents infinite recursion?", "A reachable base case.", ["A larger input", "A reachable base case", "More variables", "A print statement"], 1, "The base case ends the chain of self-calls."),
    make("stacks-queues", "Stacks and queues", "Stacks use last-in-first-out order; queues use first-in-first-out order.", "Undo history is stack-shaped; print jobs, support tickets, and breadth-first search are queue-shaped.", "Pick the data structure from the order of service. A stack pushes and pops one end. A queue adds at the back and removes from the front.", "Which structure models people waiting in a line, and why?", "A queue: the first person to arrive should be the first served (FIFO).", "What order does a stack use?", "Last in, first out.", ["First in, first out", "Last in, first out", "Sorted only", "Random"], 1, "A stack removes the most recently added item first."),
    make("trees", "Trees and hierarchical data", "A tree connects nodes in parent-child relationships without cycles; a binary search tree also maintains an ordering rule.", "File systems, page outlines, syntax trees, and organisation charts are hierarchical.", "Tree traversal means choosing a systematic visit order. In a binary search tree, an in-order traversal visits keys in sorted order because all left keys precede the node and all right keys follow it.", "Why does in-order traversal of a valid binary search tree produce sorted keys?", "It visits every smaller left-subtree key, then the node, then every larger right-subtree key.", "What is a tree good at representing?", "Hierarchy.", ["Only matrices", "Hierarchy", "Network packets only", "A single scalar"], 1, "Parents and children naturally represent nested relationships."),
    make("graphs", "Graphs and breadth-first search", "Graphs model entities and relationships; breadth-first search explores in layers from a start node.", "Maps, social connections, prerequisites, and network routing are graph problems.", "BFS uses a queue and marks nodes when discovered. In an unweighted graph, the first time it reaches a node is through a shortest number-of-edges path.", "Why mark a node when it is added to the BFS queue?", "It prevents duplicate enqueues and guarantees each node is processed once.", "Which structure does BFS use?", "A queue.", ["Stack", "Queue", "Hash only", "Matrix only"], 1, "A queue preserves the layer-by-layer order BFS requires."),
    make("dynamic-programming", "Dynamic programming", "Dynamic programming saves answers to overlapping subproblems instead of recomputing them.", "Autocomplete, sequence alignment, planning, and optimisation often reuse smaller answers.", "First define the state precisely: what does dp[i] mean? Then write a recurrence, base cases, and an evaluation order. Memorisation caches top-down calls; tabulation fills a table bottom-up.", "For climbing 1 or 2 steps at a time, what recurrence counts ways to reach step n?", "ways(n) = ways(n - 1) + ways(n - 2), with ways(0) = 1 and ways(1) = 1. The final move came from one of those two states.", "When is dynamic programming appropriate?", "When subproblems overlap and optimal structure can be reused.", ["When all answers are random", "When subproblems overlap", "Only for arrays", "Only for graphics"], 1, "Saving repeated subproblem answers is the central DP idea."),
  ] },
  { title: "Systems, Web, and Data", description: "See what happens beneath code and across a network.", topicCount: 24, topics: [
    make("binary", "Bits, bytes, and binary", "Computers represent information with bits; bytes are groups of eight bits.", "Storage, networks, colour, and machine instructions all use binary representation.", "Binary is base 2. Place values double from right to left: 1, 2, 4, 8, and so on.", "Convert binary 1011 to decimal.", "8 + 0 + 2 + 1 = 11.", "How many bits are in a byte?", "Eight.", ["2", "4", "8", "16"], 2, "A byte conventionally contains eight bits."),
    make("http", "HTTP and APIs", "HTTP defines how clients request resources and servers respond.", "Web apps and mobile apps use APIs to exchange structured data.", "A request has a method, URL, headers, and sometimes a body. A response has a status code, headers, and a body, often JSON.", "What is a 404 response communicating?", "The requested resource was not found.", "What is an API?", "An interface for software to communicate.", ["A file type", "A hardware chip", "A software interface", "A database row"], 2, "An API defines a way for programs to request or exchange capabilities/data."),
    make("memory", "Memory and references", "Memory stores program state; a reference lets a name point to an object rather than copy it.", "Shared mutable data causes many production bugs, especially across UI state and services.", "Separate the value from the name that refers to it. When two names reference the same mutable list, changing through one name is visible through the other.", "Why can changing `b` affect `a` after `b = a` for a list?", "Both names refer to the same list object. Make a deliberate copy when independent state is required.", "What does a reference represent?", "A way to access an object in memory.", ["A guaranteed copy", "A way to access an object", "A CPU core", "A network port"], 1, "References explain aliasing: multiple names can access one mutable object."),
    make("processes", "Processes, threads, and concurrency", "A process owns isolated resources; threads are execution paths inside a process that may share memory.", "Browsers, game servers, and data pipelines use concurrency to keep work responsive.", "Concurrency introduces ordering uncertainty. Protect shared state, avoid blocking the event loop, and design operations to be safe if retried.", "Why can two threads incrementing one counter produce a wrong total?", "The read-modify-write steps can interleave, so one update overwrites another without synchronisation.", "What is a race condition?", "A bug caused by timing-dependent access to shared state.", ["A faster CPU", "Timing-dependent shared-state bug", "A sorting method", "A file format"], 1, "Race conditions arise when correctness depends on an uncontrolled execution order."),
    make("databases", "Databases and queries", "A database persists structured information and answers questions through queries.", "Accounts, orders, course progress, and analytics need durable data that survives application restarts.", "Model entities, identify a stable key, and query only needed columns. Indexes speed common lookups but add cost when data changes.", "Why index a frequently searched email column?", "An index helps the database find matching rows without scanning every user, trading extra storage and write work for faster reads.", "What is a database index for?", "Faster lookup of values.", ["Encrypting every row", "Faster lookup", "Drawing a UI", "Replacing backups"], 1, "An index is an auxiliary structure that accelerates selected queries."),
    make("networking", "Networks, DNS, and latency", "Networks deliver packets between machines; DNS translates human names into addresses.", "Every cloud app depends on latency, partial failure, retries, and names resolving correctly.", "A request can fail after reaching the server, so APIs need idempotency where possible. Latency is not just distance: queues and slow dependencies matter too.", "Why might a retry accidentally create two payments?", "The first request may have succeeded but its response was lost. Without an idempotency key, the server cannot recognise the repeat.", "What does DNS do?", "Maps a domain name to an address.", ["Stores passwords", "Maps names to addresses", "Compiles code", "Sorts packets"], 1, "DNS makes names such as a website address usable by network clients."),
    make("security", "Security and threat modelling", "Security starts by identifying assets, attackers, trust boundaries, and likely abuse paths.", "Login flows, payments, student data, and shared rooms must resist misuse rather than merely work when everyone behaves.", "Validate input on the server, use least privilege, keep secrets out of code, and treat authentication separately from authorisation.", "What is the difference between authentication and authorisation?", "Authentication establishes who someone is; authorisation decides what that identity may do.", "What does least privilege mean?", "Give only the permissions necessary for a task.", ["Give all permissions", "Give only needed permissions", "Hide the UI", "Avoid backups"], 1, "Least privilege limits the damage from mistakes or compromised accounts."),
  ] },
  { title: "Math for AI and Machine Learning", description: "Use linear algebra, probability, and evaluation to think clearly about models.", topicCount: 20, topics: [
    make("vectors", "Vectors and dot products", "Vectors are ordered numeric lists; dot products measure aligned components.", "Similarity search and neural layers rely on vector operations.", "Multiply matching components and add them. For [2,3] dot [4,-1], calculate 2×4 + 3×-1.", "Compute [2,3] dot [4,-1].", "8 - 3 = 5.", "What does a dot product do?", "It multiplies matching components and sums them.", ["Sorts values", "Multiplies and sums matching entries", "Deletes a vector", "Creates a string"], 1, "The dot product combines corresponding components into one scalar."),
    make("matrices", "Matrices and transformations", "A matrix is a table of numbers that transforms an input vector into an output vector through weighted combinations.", "Graphics, image transforms, and neural-network layers are repeated matrix transformations.", "An m by n matrix takes an n-dimensional input and produces an m-dimensional output. Dimensions are not decoration: they prove whether multiplication is defined.", "A 2 by 3 matrix multiplies what length vector, and produces what length vector?", "It needs a length-3 input because it has 3 columns, and produces a length-2 output because it has 2 rows.", "What must match for matrix-vector multiplication?", "The matrix column count must equal the vector length.", ["The row count and vector length", "The column count and vector length", "All values must be positive", "Nothing"], 1, "Each matrix row takes a weighted combination of all input coordinates, so its number of columns must match the input length."),
    make("derivatives", "Derivatives as rates of change", "A derivative measures how an output changes when its input changes by a very small amount.", "Velocity, marginal cost, and model sensitivity are all rate-of-change questions.", "Zoom into a smooth graph until it looks like a line. The slope of that local line is the derivative. Always carry units with your interpretation.", "If s(t) = t squared, what is the derivative at t = 3?", "The derivative is 2t, so it is 6 at t = 3. If s is metres and t is seconds, the unit is metres per second.", "What does a derivative represent locally?", "A rate of change or slope.", ["A list length", "A local rate of change", "A random sample", "A database join"], 1, "A derivative captures the slope of a function near one input value."),
    make("gradient-descent", "Gradients and gradient descent", "A gradient collects rate-of-change information for a function with many inputs; moving opposite it can reduce a loss.", "Linear regression and neural networks are trained through repeated gradient-based updates.", "Imagine descending a foggy hill. The gradient points steepest uphill, so a cautious step in the opposite direction decreases height. The learning rate chooses how cautious the step is.", "Why can an overly large learning rate fail even with the correct gradient?", "It can leap past a low point, bounce between sides, or diverge. Direction and step size both matter.", "What does the learning rate control?", "The size of each update step.", ["The data type", "The number of classes", "The update step size", "The test set"], 2, "The learning rate scales how far parameters move on each gradient update."),
    make("probability", "Conditional probability", "Conditional probability measures an event after restricting to a condition.", "Screening, fraud, and A/B tests need base-rate aware conditional reasoning.", "P(A given B) asks about A only among outcomes where B happened. State the population before calculating.", "30 students use Python; 18 also use Git. What is P(Git given Python)?", "18/30 = 0.6 because only Python users are now in the sample space.", "What changes under conditional probability?", "The sample space is restricted by the condition.", ["The programming language", "The sample space", "All facts disappear", "Nothing"], 1, "Conditioning filters the possible outcomes to those where the condition holds."),
    make("expectation", "Expectation and variance", "Expectation is a long-run weighted average; variance measures how spread out outcomes are around that average.", "Forecasting and model-risk decisions need both an average and a sense of uncertainty.", "Expectation is the balance point of possible outcomes. Variance asks whether outcomes cluster tightly around that balance or vary widely.", "What is the expectation of a fair six-sided die?", "Add 1 through 6 to get 21, then divide by 6: 3.5. You do not roll 3.5; it is the long-run average.", "Can an expectation be a value that never occurs?", "Yes, such as 3.5 for a fair die.", ["Never", "Only for ML", "Yes", "Only after sorting"], 2, "Expectation describes a weighted average, not necessarily one possible outcome."),
    make("evaluation", "Train, validation, and test", "Separate data prevents a model evaluation from being tuned to its own exam.", "Honest evaluation protects deployment decisions in real systems.", "Fit parameters on training data; tune choices on validation data; reserve test data for a final unseen estimate.", "Why should test data not guide repeated tuning?", "It leaks into the selection process and makes final performance look overly optimistic.", "What should test data be used for?", "Final generalisation evaluation.", ["Daily training", "Final evaluation", "Feature creation", "Password storage"], 1, "The test set is held out for the final honest performance estimate."),
    make("precision-recall", "Precision, recall, and real harm", "Precision asks whether positive predictions are trustworthy; recall asks whether real positives were found.", "Medical screening, moderation, and fraud detection each value errors differently.", "A wide net catches more cases (recall) but may bring more false alarms. A narrow net can be more precise but miss real cases. Metrics express a trade-off, not a moral answer.", "Why might recall matter especially in early disease screening?", "Missing a real case can be costly, so the process should find as many genuine cases as possible. Follow-up review can manage false positives.", "What does recall measure?", "The fraction of real positive cases found.", ["Correct negative cases", "Positive predictions that are right", "Real positives found", "Average model speed"], 2, "Recall is true positives divided by all actual positives."),
    make("features", "Features and targets", "Features are information used to make a prediction; the target is the outcome the model should learn.", "Delivery estimates may use distance and traffic as features to predict arrival time.", "Define the prediction moment first. A feature is valid only if it would have been known then; otherwise it leaks future information.", "Why is tomorrow's final price invalid as a feature for predicting tomorrow's price today?", "It is unavailable at prediction time, so it leaks the answer into training.", "What is a target?", "The outcome a model is trained to predict.", ["A random column", "The prediction outcome", "A chart colour", "A server"], 1, "The target is the labelled answer the model learns from."),
    make("baselines", "Baselines and simple models", "A baseline is a deliberately simple comparison that a new model must beat.", "A weather model should beat predicting the historical average; otherwise its complexity adds no value.", "Start with a majority-class classifier, mean prediction, or simple rule. Measure it with the same split and metric as the advanced model.", "Why build a baseline before a neural network?", "It tells you whether the advanced system creates meaningful improvement over a cheap, understandable alternative.", "What must a new model do?", "Beat a relevant baseline on an honest evaluation.", ["Use more GPUs", "Beat a relevant baseline", "Avoid a test set", "Predict perfectly"], 1, "A baseline gives performance claims a concrete reference point."),
    make("overfitting", "Overfitting and regularisation", "Overfitting happens when a model learns accidental training patterns instead of general structure.", "A model that memorises past applicants can fail badly on new applicants.", "Compare training and validation behaviour. Regularisation, simpler models, more representative data, and early stopping can reduce overfitting, but no trick replaces honest data.", "What pattern suggests overfitting?", "Very strong training performance with noticeably weaker validation performance.", "What is overfitting?", "Learning training noise rather than general patterns.", ["Fast inference", "Learning noise", "Better validation", "Data storage"], 1, "An overfit model performs well on familiar examples but generalises poorly."),
    make("classification", "Classification and thresholds", "Classification assigns categories; a threshold turns a score into a yes/no decision.", "Spam filters and fraud systems score risk, then choose a threshold based on the cost of errors.", "Changing a threshold changes precision and recall. Select it with domain owners and validation data, never because one metric looks flattering alone.", "What usually happens to recall when you lower a positive-class threshold?", "More cases are labelled positive, so recall often rises while false positives can also rise.", "What does a classification threshold do?", "Converts a score into a category decision.", ["Encrypts data", "Converts a score to a decision", "Creates features", "Sorts labels"], 1, "Thresholds operationalise a score into an action."),
  ] },
];










